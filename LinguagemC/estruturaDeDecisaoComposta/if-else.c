#include <stdio.h>

int main() {

    // Exemplo simples de estrutura de decisão if-else
    int numero = 10;
    if (numero % 2 == 0) {
        printf("O numero %d e par.\n", numero);
    } else {
        printf("O numero %d e impar.\n", numero);
    }
    
    return 0;
}